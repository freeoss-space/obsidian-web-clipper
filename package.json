{
	"name": "obsidian-web-clipper-mobile",
	"version": "0.0.3",
	"description": "Clip web pages from mobile share intents into Obsidian notes",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"keywords": [],
	"author": "",
	"license": "GPL-3.0-or-later",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.20.0",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.3.3",
		"vitest": "^4.1.0"
	},
	"dependencies": {
		"@types/turndown": "^5.0.4",
		"turndown": "^7.1.3"
	}
}
