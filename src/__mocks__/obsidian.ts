// Minimal stub of the Obsidian API for unit tests.
// Only the identifiers actually imported by tested source files need to be present.
export const requestUrl = async (_opts: unknown) => ({ text: '' });
